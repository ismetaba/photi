# Mimari
2026-05-09 — Queue depth low (only T12 pending, builder warming up). T12 is fat but spec-aligned, leaving alone. Added T13 (job-runner crash recovery — real reliability gap, no task covered it), T14 (e2e happy-path integration test spanning T03-T09), T15 (demo seed script for the README manual QA scenario). All additive, all post-T12.
2026-05-09 — Builder picked up T15, leaving zero pending. Added T16 (upload mime-type allow-list on photos + selfie endpoints) — silent skip on non-images is a real ingestion hygiene gap not covered by any prior task.
2026-05-09 — T16 now in_progress, zero pending. Added T17 (413 + per-part oversize handling — current 25MB limit surfaces as 500) and T18 (broadcast `photo-ready` from processPhoto so foyer reflects featured-then-processed photos — type was declared but never emitted). Both additive reliability/UX gaps.
2026-05-09 — T18 now in_progress, zero pending. Added T19 (mirror `photo-ready` broadcast on matchSelfie + retryAwaiting paths) — same staleness bug repeats on the awaiting_credit→ready transition; T18 only fixes processPhoto.
2026-05-09 — T19 done, zero pending. Added T20 (broadcast `photo-removed` on DELETE /photos/:id — third side of the foyer staleness coin: featured photo deletion leaves a 404'd `<img>` on the TV until next refetch) and T21 (block upload + isFeatured + delete on archived events — `archived` status was wired but not enforced anywhere in the photo routes). Both additive product gaps.
2026-05-09 — Inspection cycle: T01-T21 all done; auditor pre-queued A1-1 (cover-image input, in_progress) + A1-2 (foyer QR header, pending) covering the only two ✗ gaps from Cycle 1 audit. Queue is sufficient — 1 in_progress + 1 pending exhausts the verified spec gap list. No new tasks added; manufacturing more would pollute the plan.
2026-05-09 — Re-inspection: A1-1 done, A1-2 now in_progress, zero pending. Spec is fully covered per Cycle 1 audit; both ✗ gaps are closed or in-flight. Holding the line — no new tasks. Speculative additions after a clean audit would dilute, not improve, the plan.
2026-05-09 — Third inspection: A1-2 still in_progress (Foyer.tsx fetches qrUrl but hasn't rendered the <img> yet — matches the task scope). No remaining spec gaps verified against code. Holding line again; no tasks added.
2026-05-09 — Fourth inspection: A1-2 builder has now rendered the QR <img> in Foyer header (lines 165-178) — task still in_progress pending backend public qr.png route + tests. All Cycle 1 ✗ gaps closed-or-in-flight, no new spec gap surfaces from re-reading code. No changes.
2026-05-09 — Fifth inspection: A1-2 substantively complete on disk (Foyer.tsx renders QR <img> + backend `GET /events/:slug/qr.png` route at events.ts:215 + 200/404 public tests at events-route.test.ts:217-242) — only awaiting Builder's status flip. Spec re-walked, no uncovered ✗ gaps. Holding line; no tasks added.
2026-05-09 — Sixth inspection: A1-2 still in_progress with same on-disk completion as cycle 5. Spec audit clean; "Kapsam Dışı" explicitly forecloses real-auth/payment/cloud-storage/i18n/audit-log additions. Holding line — no changes needed.
2026-05-09 — Seventh inspection: identical state — A1-2 on-disk-complete, status still in_progress (Builder's domain). All audited ✗ gaps closed-or-in-flight; spec carries no untouched ✓ gap. Holding line.
2026-05-09 — Eighth inspection: A1-2 now flipped to done — full queue T01-T21 + A1-1 + A1-2 all done, zero pending/in_progress. Audit log carries explicit AUDIT_PASS. Spec fully satisfied; further tasks would be speculative scope creep. Holding line — no changes.
